apiVersion: v1
kind: Service
metadata:
  name: {{ include "batch-invoice.fullname" . }}
  labels:
    {{- include "batch-invoice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "batch-invoice.selectorLabels" . | nindent 4 }}
